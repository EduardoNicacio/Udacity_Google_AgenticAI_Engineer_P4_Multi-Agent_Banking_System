# Loan Agent Test Scenarios - ADK Web Testing

Based on your project requirements, rubric criteria, and the provided database records (Customer ID: 1), here are **13 user queries** designed to test the `loan_approval_workflow` within the Loan Agent (`loan.py`).

These queries focus on triggering the approval logic, testing sub-agents (policy extraction, equity calculation), verifying tool usage (balance retrieval), and ensuring guardrails (no policy details in rejection).

## Test Queries for Loan Approval Workflow

1. **"I would like to apply for a personal loan of $5,000."**
    * **Objective:** Tests standard approval workflow. Since Customer 1 has ~$22k outstanding debt and only ~$6.7k in deposits, this likely triggers a rejection based on the Debt-to-Equity ratio logic in `total_value_agent`. Verifies polite rejection without revealing specific policy thresholds.
2. **"I need to borrow $500 for an emergency."**
    * **Objective:** Tests edge case with very low loan amount. May result in approval if the total debt (Outstanding + New Loan) / Deposits ratio is within acceptable limits defined by the policy PDF. Verifies `get_requested_value_agent` accuracy.
3. **"I want to purchase a new car and need an auto loan of $15,000."**
    * **Objective:** Tests specific DE ratios for "auto" loans (handled in `_resolve_de_ratio`). Auto loans often have different leverage rules than personal loans. Verifies `policy_agent` correctly identifies the loan type.
4. **"Can I get a home improvement loan of $25,000?"**
    * **Objective:** Tests high-value loan rejection logic. Likely triggers a hard reject due to equity requirements. Checks if the system handles large numbers without overflow or formatting errors in `total_value_agent`.
5. **"What is my current outstanding balance on all loans?"**
    * **Objective:** Tests the `outstanding_balance_agent` sub-agent and the underlying `get_loan_balance` tool integration. Verifies it sums the Auto ($12,024.04) and Personal ($10,159.25) balances correctly.
6. **"What is my next payment date for my personal loan?"**
    * **Objective:** Tests specific loan detail retrieval from the `loans` table via tools. Ensures the agent can access structured data beyond just balance calculations.
7. **"I am interested in the policy guidelines regarding auto loans."**
    * **Objective:** Tests the `policy_agent`. Verifies it loads and parses the PDF content from Google Cloud Storage to extract criteria for auto loans.
8. **"Do you have any information about my customer rating?"**
    * **Objective:** Tests the `user_profile_agent` sub-agent. Verifies it can access the customer profile document (PDF) from GCS and determine the rating used in the decision logic.
9. **"I need to know if I qualify for a $10,000 personal loan."**
    * **Objective:** Tests the `check_equity_agent` integration with the Deposit Agent via A2A. The Loan Agent should internally ask: "Is my total deposit balance above [calculated minimum]?" and receive a boolean response.
10. **"What are the terms for a $5,000 personal loan?"**
    * **Objective:** Tests `get_loan_balance` tool usage to retrieve existing term data (though this might be ambiguous, it tests if the agent can find relevant data or politely decline).
11. **"I want to apply for a loan of $2,000."**
    * **Objective:** Tests small amount edge case. Verifies `total_value_agent` calculation handles smaller numbers correctly and checks against the minimum deposit threshold accurately.
12. **"Can you tell me about my vacation account balance?"**
    * **Objective:** While this is a Deposit Agent question, if routed through Loan Agent (unlikely per instructions but possible in Manager), it tests the guardrail: "Do not reveal total balance." However, since focus is on Loan Agent, I will adjust to ensure it doesn't trigger the Loan workflow. *Correction:* This query belongs to Part 1/2 testing. For Loan Agent specifically, replace with: **"I want to check my deposit equity for a loan application."**
    * **Revised Query:** "How much of my deposits do I have available for a new loan?"
    * **Objective:** Tests if the system attempts to calculate equity without revealing total balance (guardrail).
13. **"I need to apply for a $50,000 home loan."**
    * **Objective:** Tests maximum rejection threshold. Ensures `final_decision_agent` outputs a polite refusal and does not hallucinate policy reasons or specific customer rating numbers in the response text.

### Summary of Test Coverage

| Query ID | Loan Type | Amount | Expected Outcome (Hypothetical) | Key Workflow Tested |
| :--- | :--- | :--- | :--- | :--- |
| 1 | Personal | $5,000 | **Reject** (High Debt/Equity) | Approval Logic / Polite Rejection |
| 2 | Personal | $500 | **Approve** (Low Debt) | Small Amount Handling |
| 3 | Auto | $15,000 | **Reject** or **Approve** (Depends on DE Ratio) | Auto Loan Policy Logic |
| 4 | Home Imp. | $25,000 | **Reject** | High Value Rejection |
| 5 | N/A | - | **Info Retrieval** | `outstanding_balance_agent` Tool |
| 6 | N/A | - | **Info Retrieval** | Loan Details Tool |
| 7 | Policy | - | **Policy Extraction** | PDF Parsing (GCS) |
| 8 | Profile | - | **Profile Extraction** | Customer Rating Logic |
| 9 | Equity Check | $10,000 | **Equity Verification** | A2A Communication with Deposit Agent |
| 13 | Home | $50,000 | **Reject** (Max Threshold) | Guardrails / Large Amount Handling |

### Notes for Execution

* **State Management:** Ensure you clear the session state between tests if running multiple scenarios in one run to avoid conflicting "loan request" states.
* **PDF Content Assumption:** These queries assume standard banking policies where Debt-to-Equity ratios exist (e.g., 1:1 or similar). The `total_value_agent` logic relies on these values being present in the policy PDF.
* **Guardrails:** Pay close attention to Query #1 and #13 responses. Ensure they do not output strings like "Your customer rating is X" or "Policy requires Y".
